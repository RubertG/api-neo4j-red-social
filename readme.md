# Api Rest con FastApi y Neo4j en Python
- Python
- Neo4j
- FastApi

# Paquetes instalados
```
pip install uvicorn 
pip install uvicorn[standard]
pip install neo4j
pip install python-dotenv
pip install fastapi
```

# Levantar Servidor FastApi
```
uvicorn main:app --reload
```